/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  crossOrigin: false,
  compress: true,
  poweredByHeader: false,
};

module.exports = nextConfig;
